"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decode = exports.encode = void 0;
var chars = "0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ!#$%()*+,-./\\:;=?@[]^_`{|}~\"'&<> ";
function encode(str) {
    if (str === void 0) { str = ""; }
    var r = "";
    for (var _i = 0, _a = String(str); _i < _a.length; _i++) {
        var l = _a[_i];
        if (chars.indexOf(l) < 0) {
            r += String(chars.length + 1);
            continue;
        }
        r +=
            chars.indexOf(l) + 1 < 10
                ? "0" + (chars.indexOf(l) + 1)
                : "" + (chars.indexOf(l) + 1);
    }
    return r + "00";
}
exports.encode = encode;
function decode(num, startLetter) {
    if (startLetter === void 0) { startLetter = 0; }
    var r = "";
    var t = String(num)
        .slice(startLetter)
        .match(/[0-9][0-9]?/g);
    for (var _i = 0, t_1 = t; _i < t_1.length; _i++) {
        var c = t_1[_i];
        if (c === "00") {
            return r;
        }
        if (c > chars.length) {
            continue;
        }
        r += chars[c - 1];
    }
    return r;
}
exports.decode = decode;
